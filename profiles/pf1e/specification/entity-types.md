# Entity Types

The initial PF1e profile defines four entity types.

- `pf1e.entity.feat` for feat nodes and parameterized feat variants
- `pf1e.entity.class` for class identifiers used by class-level requirements
- `pf1e.entity.skill` for skill identifiers and taxonomy alignment
- `pf1e.entity.source` for bibliographic anchors
