# Selections

PF1e `0.1.0` supports two parameter kinds.

- `weapon`
- `spell-school`

Selection values are validated against the profile feat schema. Follow-up feats such as `Greater Spell Focus` point to the concrete prerequisite feat entity for the same selected value.
